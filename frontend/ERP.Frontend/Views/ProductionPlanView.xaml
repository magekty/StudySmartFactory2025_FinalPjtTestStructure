<UserControl x:Class="ERP.Frontend.Views.ProductionPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ERP.Frontend.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="생산 계획" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" Grid.Row="0" />

        <DataGrid Grid.Row="1" ItemsSource="{Binding ProductionPlans}" AutoGenerateColumns="False" Margin="0,10,0,0" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="계획 ID" Binding="{Binding PlanId}" />
                <DataGridTextColumn Header="제품명" Binding="{Binding Product.Name}" />
                <DataGridTextColumn Header="수량" Binding="{Binding Quantity}" />
                <DataGridTextColumn Header="시작일" Binding="{Binding PlannedStart, StringFormat='{}{0:yyyy-MM-dd}'}" />
                <DataGridTextColumn Header="종료일" Binding="{Binding PlannedEnd, StringFormat='{}{0:yyyy-MM-dd}'}" />
                <DataGridTextColumn Header="상태" Binding="{Binding Status}" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="계획 등록" Command="{Binding AddPlanCommand}" Padding="10" />
        </StackPanel>
    </Grid>
</UserControl>