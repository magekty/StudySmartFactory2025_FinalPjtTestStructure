<UserControl x:Class="ERP.Frontend.Views.CostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ERP.Frontend.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="원가 계산" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" Grid.Row="0" />

        <StackPanel Grid.Row="1" Margin="10">
            <TextBlock Text="생산 계획 선택" FontWeight="Bold" Margin="0,0,0,5" />
            <ComboBox ItemsSource="{Binding ProductionPlans}" 
                      DisplayMemberPath="ProductName"
                      SelectedItem="{Binding SelectedProductionPlan}" />

            <Grid Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="인건비 (고정값):" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0" />
                <TextBox Text="{Binding LaborCostRate}" Grid.Row="0" Grid.Column="1" />

                <TextBlock Text="제조 경비 (고정값):" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,10,10,0" />
                <TextBox Text="{Binding ManufacturingOverheadRate}" Grid.Row="1" Grid.Column="1" Margin="0,10,0,0" />

                <Button Content="원가 계산" Command="{Binding CalculateCostCommand}" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,20,0,0" />

                <TextBlock Text="{Binding TotalCostResult}" FontSize="20" FontWeight="Bold" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,20,0,0" />
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>